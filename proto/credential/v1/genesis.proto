syntax = "proto3";
package elestodao.elesto.credential.v1;

option go_package = "github.com/elesto-dao/elesto/v2/x/credential";


// GenesisState defines the credential module's genesis state.
message GenesisState {
  // allowed credential ids present at genesis
  repeated string allowed_credential_ids = 1;
}
